[pytest]
pythonpath = .
testpaths = tests
addopts = -q
markers =
    slow: fits DoWhy/EconML models (slower; the causal-method validations)
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
