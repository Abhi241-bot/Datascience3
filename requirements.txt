# Core numerics
# NOTE: deploy on Python 3.12 (Streamlit Cloud -> Advanced settings). The cvxpy /
# scikit-learn / scipy stack has no wheels for 3.13+/3.14, so a newer interpreter
# forces a from-source build that fails on Streamlit Cloud.
numpy>=1.26
pandas>=2.0
scipy>=1.11

# Statistics
statsmodels>=0.14

# Causal inference (Phase 4)
dowhy>=0.12          # pulls cvxpy>=1.7.1 (needs a wheel -> pin Python 3.12)
econml>=0.15
scikit-learn>=1.3
# DoWhy 0.14 still calls networkx.algorithms.d_separated, which was removed in
# networkx 3.5. Cap below 3.5 so PSM works on the deployed environment.
networkx>=3.0,<3.5

# App + plotting
streamlit>=1.30
plotly>=5.18
matplotlib>=3.7

# Tests
pytest>=7.4
